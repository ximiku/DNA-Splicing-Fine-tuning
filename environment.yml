name: dna-fine-tune
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - --extra-index-url https://download.pytorch.org/whl/cu128
      - torch>=2.7.0
      - transformers>=4.37,<5
      - datasets>=2.18
      - accelerate>=0.27
      - scikit-learn>=1.4
      - pandas>=2.1
      - numpy>=1.26
      - pyfaidx>=0.8
      - tqdm>=4.66
      - PyYAML>=6.0
      - pyarrow>=15.0
      - matplotlib>=3.8
      - joblib>=1.3
      - einops>=0.7
